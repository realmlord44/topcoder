#include <vector>
#include <string>
using namespace std;

class ATaleOfThreeCities {
public:
  double connect(vector<int> const &ax,
                 vector<int> const &ay,
                 vector<int> const &bx,
                 vector<int> const &by,
                 vector<int> const &cx,
                 vector<int> const &cy) {
    return 0;
  }
};
